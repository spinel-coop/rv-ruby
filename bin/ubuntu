#!/bin/bash

PLATFORM=${1:-arm64}

if docker ps --all | grep "jdx-ubuntu-$PLATFORM" > /dev/null; then
  set -v
  docker start -a "jdx-ubuntu-$PLATFORM"
else
  set -v
  docker run -it --name "jdx-ubuntu-$PLATFORM" --volume .:/src --platform "linux/$PLATFORM" homebrew/ubuntu22.04
fi
